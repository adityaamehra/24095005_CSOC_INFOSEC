challenge 2:-

1.) information :- Got the cat.jpg , got the exif data and saw something like base64 in the license region , saw it whole in the hexdump got "cGljb0NURnt0aGVfbTN0YWRhdGFfMXNfbW9kaWZpZWR9" which on base64 got me "picoCTF{the_m3tadata_1s_modified}"

2.) Matryoshka doll :- got the image and because of the name Matryoshka i got the reference with the "-M" aka Matryoshka mode in binwalk so used the command "binwalk -e -M dolls.jpg" and got the flag in the flag.txt being "picoCTF{bf6acf878dcbd752f4721e41b1b1b66b}"

3.) tunn3l v1s10n :- I saw that the file is not being able to be opened so looked through it using "hexedit" and found "https://www.donwalizerjr.com/understanding-bmp/" this and say that the 14th bit should be 28 changed it and got an image with flag "notaflag{sorry}" and then i proceeded to change the 22 and 23rd bit to 6E 04 and got the flag "picoCTF{qu1t3_a_v13w_2020}".

4.) MacroHard WeakEdge :- I first used the binwalk command on the pptm file and got that it is a zip file instead opened the zip file and extracted it , when through the directories and found a file named hidden copied the text inside it and used base64 decode and found the flag "picoCTF{D1d_u_kn0w_ppts_r_z1p5}".

5.) Enhance! :- I opened the svg file and saw that it opened in the google chrome , i instinctively opened the inspection panel and saw that there is some text field there , i opened and the flag was there written out "picoCTF{3nh4nc3d_d0a757bf}"

6.) advanced-potion-making :- I tried to open the file and it was corrupted then went on to see the hex dump of the file and saw that the magic bytes for the png file are not in place , changed them and the file started to open , but still no flag to be seen , then opened gimp software change the background to black and white and the flag was visible "picoCTF{w1z4rdry}"

7.) File types :- I used the file command and found that the file given is a shell archive to ran it using the command "chmod +x a" and "sh a" , then it created a file named flag , found out that it was ar archive , then used the command "ar x flag" , created a cpio archive and then unarchived it using "cpio -ivd < flag" made a file which is bzip2 archive on unarchivng using "bunzip2 a" created a.out which is gzip compressed data decompressed using "gzip -d a.gz" then it created a lzip file decompressed it using "lzip -d a" giving a LZ4 compressed file decompressing using "lz4 -d a" giving a LZMA compressed file then decompressed it using "lzma -d a.lzma" giving me a lzop file decompressed it using "lzop -d a.lzop" giving me a lzip compresse file and then XZ compressed file decompressed it using "xz -d a.xz" resulting in a text file containing "7069636f4354467b66316c656e406d335f6d406e3170756c407431306e5f6630725f3062326375723137795f33633739633562617d0a" when decoded gives the flag "picoCTF{f1len@m3_m@n1pul@t10n_f0r_0b2cur17y_3c79c5ba}"

8.) hideme :- I used the command "binwalk -e -M flag.png" made a new png file and the flag on it was "picoCTF{Hiddinng_An_imag3_within_@n_ima9e_82101824}"

9.) MSB :- This challenge can be used by downloading a utility "sigbits.py" which does lsb and msb for us downloadable at "https://github.com/Pulho/sigBits" , i used it and grepped the output file and got the flag as "picoCTF{15_y0ur_que57_qu1x071c_0r_h3r01c_3a219174}"

10.) extensions :- Opened the txt file saw the 'PNG' magic bytes knew it was png image file changed the extension and got the flag "picoCTF{now_you_know_about_extensions}".